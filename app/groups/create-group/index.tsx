import React from 'react';
import {observer} from "mobx-react-lite";
import CreateEditGroup from "@/components/ui/create-edit-group";

const CreateGroup = observer(() => {
  return <CreateEditGroup/>
})

export default CreateGroup;